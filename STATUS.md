# STATUS — festival-morozhenoe

**Обновлено:** 2026-08-05
**Стадия:** ✅ работает end-to-end. Лого + тексты + плакат по правкам клиентки внесены и на проде. Ждём один ответ (убирать ли строку-бейдж на лендинге) — см. «Открытый вопрос».

Живой лендинг: **https://dungeonmaster337.github.io/festival-morozhenoe/**
Репо (public): github.com/DungeonMaster337/festival-morozhenoe
Бренд: **айскро** = ООО «ИЛЬ МИО МОРОЖЕНКО», сайт icecro.ru.

## Что готово и работает
- **Лендинг** (`index.html`): бирюза айскро (акцент `#057A8A`, бренд-бирюза `#00B4CC`, тёплый жёлтый `#F2B705`), обе темы, WCAG AA, мобайл. Слот под логотип вверху (`.logo-slot` — заглушка).
- **Форма → Google-таблица.** sendBeacon (fire-and-forget, no-cors) → Apps Script → таблица. Работает на десктопе и телефоне. Единственный источник данных = таблица (панель организатора = сама таблица под аккаунтом). Локального хранилища/вкладки «Организатору» НЕТ.
- **Билет** = код из 6 символов (`newTicket()`, набор без похожих O/0/I/1), напр. `9DC-F7K`. Генерится телефоном, шлётся в таблицу и показывается — экран и колонка «Билет» = ОДИН код. Колонка `#` = сквозной счёт 1,2,3.
- **Поле «Выпавший вкус»** (select, обязательное). Список — ЗАГЛУШКА, сверить с реальным колесом.
- **Согласие ПДн** корректное: оператор ООО «ИЛЬ МИО МОРОЖЕНКО» (ИНН 7725692792) + ссылка на их политику https://icecro.ru/include/content/privacy_policy.php.
- **Офлайн:** `navigator.onLine===false` → честная ошибка `#formErr` (не фейковый успех).
- **Печать:** `plakat-a6-print.pdf` — готовый A6 (105×148 мм, full-bleed, всё влезает). Плюс `plakat-a6.html`, `qr-festival.png`.

## Ключевые артефакты
- Таблица: `1w2z0UF1JXVwCTfheftx4CwZfgN1u_shjsnLmb-OTfaI`
- Apps Script endpoint: в `index.html` (`SHEET_ENDPOINT`). Развёрнутый код = `tools/gsheet-endpoint.gs` (колонки `# | Билет | Имя | Телефон | Email | Согласие реклама | Выпавший вкус | Дата/время`).

## ✅ Внесено 2026-08-05 (её сообщения фото 1/2)
- Плакат: «Выиграй годовой запас мороженого от айскро» / «Отсканируй код, зарегистрируйся за 10 сек…» / футер «Итоги розыгрыша — в 19:00 на Центральном рынке».
- Экран успеха: «…выберем победителя с помощью рандомайзера» + строка подписки @icecro_official.
- `plakat-a6-print.pdf` пересобран из актуального макета (Chrome 150 headless → векторный A6 105×148мм, QR ведёт на прод, всё влезает). Задеплоено на Pages.

## ✅ Логотип + правки плаката (фото 3) — 2026-08-05
- `logo.png` (бирюза на прозрачном, фирменный, с сердечком) — на лендинге в шапке (`.brandbar`) и вверху плаката. Задеплоено, проверено на проде (logo.png → 200).
- **Casing решён:** логотип строчными «айскро» → везде строчными.
- **Плакат (фото 3):** убрана строка-eyebrow «Фестиваль мороженого · Центральный рынок» (она зачеркнула красным). Заголовок = её текст «Выиграй годовой запас мороженого от айскро!» (с «!»). PDF пересобран, всё влезает, QR ведёт на прод.

## ⏳ ОТКРЫТЫЙ ВОПРОС — ждём ответ Ксении (Дмитрий отправил 2026-08-05)
- **Строка-бейдж на ЛЕНДИНГЕ**: «● Фестиваль мороженого · Центральный рынок» (`.eyebrow` в `index.html`, ~стр.221) — такая же, как убранная с плаката. На плакате убрали, на лендинге ПОКА ОСТАВИЛИ. Спросили: убрать и там? Если да — удалить `<span class="eyebrow">…</span>` в index.html, задеплоить.

## ⏳ Осталось (не из её сообщений)
1. **Список вкусов** в форме — сверить с реальным колесом (заглушка).
2. Перед боевым днём — удалить тестовые строки из таблицы (Дмитрий/Полина/Андрей).

## TODO перед боевым днём
- **Удалить тестовые строки** из таблицы (Дмитрий/Полина/Андрей — тесты).
- Решить адрес: оставить github-ссылку или свой домен/поддомен.

## ⚠️ Грабли/решения (важно, не переоткрывать)
- **Браузер НЕ читает ответ Apps Script** (302-редирект ломает CORS-чтение, хотя ACAO:* есть). Поэтому номер генерит клиент и шлёт в таблицу — НЕ пытаться читать `n` из ответа.
- **`await fetch` к Apps Script ВИСНЕТ на мобильных** (редирект) → окно успеха не показывалось. Решение: `sendBeacon`, ответ не ждём.
- **`@page size:A6` браузеры при печати игнорируют** (кладут макет в угол A4). Для печати = готовый `plakat-a6-print.pdf` (собран: Chrome headless → PDF Letter → PyMuPDF crop карточки → A6 PDF).
- **Кеш GitHub Pages:** при тесте открывать с `?v=N`, иначе браузер отдаёт старую версию.
- **`Реквизиты.pdf`** (банковские данные) — в `.gitignore`, НЕ коммитить. `!plakat-a6-print.pdf` — исключение, его коммитим.
- Реклама-галочка НЕ предзаполнена (38-ФЗ) — не менять на pre-checked.
- Редкий край: гость пересабмитит → дубль строки (дедупа нет). Опц. фикс: серверный дедуп по телефону в Apps Script (передеплой).

## Цена
- **8 000 ₽ под ключ**, названа только руководителю. **Ксении цену не дублировать.**
