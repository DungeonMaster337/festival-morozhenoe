# STATUS — festival-morozhenoe

**Обновлено:** 2026-08-03
**Стадия:** демо отправлено клиенту, ждём согласие + доступы.

## Сделано
- [x] Мобильный лендинг (`index.html`), обе темы, a11y, self-check телефона (`?selftest=1`).
- [x] **Google-таблица подключена (2026-08-04).** Форма шлёт `fetch(SHEET_ENDPOINT, no-cors)`
      → Apps Script → таблица `1w2z0UF1JXVwCTfheftx4CwZfgN1u_shjsnLmb-OTfaI`. Проверено чтением
      таблицы: строки пишутся, кириллица чистая. localStorage оставлен как офлайн-резерв.
      ⚠️ Номер билета на экране — локальный (per-device), в таблице своя нумерация по строкам.
      ⚠️ Нет авто-ретрая при офлайне (заявка лежит в localStorage, синк вручную через вкладку).
      TODO клиенту: удалить 3 тестовые строки. Реальный тест — скан телефоном на месте.
- [x] **Выбор вкуса + уникальный номер (2026-08-04).** В форме select «Выпавший вкус»
      (обяз.) → колонка таблицы (скрипт уже принимал `flavor`, передеплой НЕ нужен).
      Номер билета = номер строки таблицы (сервер возвращает `n`, ACAO:* → браузер читает).
      Уникален, совпадает с таблицей → победителя тянуть по строке. Список вкусов —
      ЗАГЛУШКА, свериться с реальным колесом.
- [x] **Убрана вкладка «Организатору» + весь localStorage (2026-08-04).** Модель: гости
      регистрируются с ЛИЧНЫХ телефонов → локальное хранилище бессмысленно (у каждого своя
      заявка, организатор их не соберёт). Единый источник = Google-таблица (панель = сама
      таблица под аккаунтом). Отправка ОНЛАЙН-ТОЛЬКО: сбой/нет сети → честная ошибка `#formErr`,
      гость повторяет (фейкового успеха нет). Номер билета = `data.n` без ведущих нулей (= строка).
      ⚠️ Редкий край: запись прошла, а ответ не прочитался → гость повторит → дубль в таблице.
      Фикс при желании: серверный дедуп по телефону в Apps Script (нужен передеплой).
- [x] **Номер билета = код, генерируемый телефоном (2026-08-04).** ПОДТВЕРЖДЕНО клиентом:
      браузер НЕ читает ответ Apps Script (редирект ломает CORS) → сервер-нумерация на экран
      не выходит (гость видел `hdy-345`, а таблица писала `1`). Решение: `nextTicket()` =
      префикс устройства (буквы без O/0/I/1) + счётчик → код `F7K-001`, шлётся в таблицу и
      показывается — экран и таблица = ОДИН код. Запись no-cors + await (офлайн → честная ошибка).
      ⏳ ТРЕБУЕТСЯ ПЕРЕДЕПЛОЙ: обновлён `tools/gsheet-endpoint.gs` (колонки #|Билет|…). Клиенту:
      вставить новый код → Deploy → Manage deployments → Edit → New version; лист очистить ПОЛНОСТЬЮ
      (шапка пересоздастся). До передеплоя код-билет в таблицу не пишется (старая структура).
- [x] **ПОДТВЕРЖДЕНО РАБОТАЕТ end-to-end (2026-08-04).** Клиент передеплоил + очистил лист.
      Таблица прочитана: шапка `# | Билет | Имя | Телефон | Email | Согласие реклама |
      Выпавший вкус | Дата/время`, строка `1 | 6RJ-002 | Дмитрий | … | Мадаг.ваниль`. Билет
      на экране == колонке «Билет». TODO клиенту: удалить тестовую строку «Дмитрий» перед запуском.
      Сообщение Ксении с запросом лого/текста/даты/вкусов — в `messages.md` (черновик #4).
- [x] **Ребрендинг в бирюзу (2026-08-04):** палитра айскро — акцент `#057A8A` (WCAG AA 5:1),
      фирменная бирюза `#00B4CC`, тёплый акцент жёлтый `#F2B705`. Все 4 темы перекрашены.
      Добавлен слот под логотип (`.logo-slot` вверху). ⏳ **Ждём:** файл логотипа (`logo.svg`/`png`
      рядом с index.html → заменить заглушку на `<img class="logo" src="logo.svg" alt="айскро">`)
      + финальный текст от клиента. Скриншот НЕ снят — Chrome-MCP был недоступен.
- [x] Задеплоен на GitHub Pages, проверен HTTP 200, title корректный.
- [x] QR-код (`qr-festival.png`) + плакат A6 (`plakat-a6.html`). Копии на Desktop.
- [x] Отправлено клиенту: (1) сообщение руководителю с ценой, (2) убеждающее сообщение
      Ксении, (3) ответ на её голосовое с разбором механики. Все — в `messages.md`.

## Цена (уже названа)
- **8 000 ₽ под ключ** (лендинг + база + размещение + QR). Ниже их потолка 10к.
- Апселлы (озвучены как опции, НЕ в базовой цене):
  - +2 000 ₽ — автоответ гостю по SMS/email после регистрации.
  - поле «выпавший вкус» (какое мороженое выпало на колесе) — предложено бесплатной опцией, полезно маркетингу.

## ⏳ Ждём от клиента (чтобы стартовать боевую версию)
1. Согласие / «да, берём».
2. **Google-аккаунт** — под общую таблицу заявок.
3. ✅ **Реквизиты оператора ПДн — получены (2026-08-04).** Оператор: ООО «ИЛЬ МИО
   МОРОЖЕНКО», ИНН 7725692792, ОГРН 1107746310572. Политика ПДн у них уже есть:
   https://icecro.ru/include/content/privacy_policy.php — вписаны в согласие формы.
   Сайт бренда: icecro.ru. ⚠️ PDF с банковскими реквизитами — в `.gitignore`, НЕ в репо.
4. Финальный адрес: оставляем github-ссылку или вешаем на их домен/поддомен.

## 🚀 Боевой запуск (делать, когда придёт «да» + доступы)
1. Создать Google-таблицу. Вставить `tools/gsheet-endpoint.gs` в Apps Script,
   Deploy → Web app → Execute as me, Access: Anyone → скопировать URL веб-приложения.
2. В `index.html` в обработчике submit заменить localStorage-сохранение на
   `fetch(WEBAPP_URL, {method:'POST', body: ...})` — POST заявки в таблицу.
   (На GitHub Pages fetch к script.google.com разрешён — CSP-ограничения артефакта тут НЕТ.)
3. Оставить localStorage как резерв на случай офлайна телефона (не терять заявку).
4. Проставить в тексте согласия реквизиты оператора + ссылку на политику.
5. Передеплой Pages, **реальный тест телефоном**: скан QR → регистрация →
   строка появилась в таблице. Проверить кириллицу.
6. Отдать клиенту финальный QR + доступ к таблице.

## ⚠️ Честные заметки (не забыть)
- Демо сейчас пишет в **localStorage** (на устройстве), НЕ общая база. Общая таблица =
  оплаченная production-часть. Клиенту так и подано (описываем конечный продукт).
- Реклама-галочка НЕ предзаполнена — по 38-ФЗ согласие добровольное. Не менять на pre-checked.
- QR прямо здесь расшифровать нечем — единственная настоящая проверка = скан телефоном.
